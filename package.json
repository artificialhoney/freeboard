{
  "name": "freeboard",
  "title": "freeboard",
  "version": "1.2.0-dev",
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up -d --force-recreate && concurrently --kill-others  \"npm run dev --workspace=packages/ui\" \"npm run dev --workspace=packages/api\"",
    "lint:fix": "eslint --fix"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "concurrently": "^9.1.0",
    "eslint": "^9.15.0",
    "globals": "^15.12.0"
  },
  "workspaces": [
    "packages/ui",
    "packages/api"
  ]
}
